##LISTA DE COMANDOS##

git reset --soft HEAD-1      para eliminar el ultimo commit * -   -

git remote   *Para ver los remotes

git remote -v   *Para ver la url del repo remoto

code .     *Para ver el còdigo



=============================================================================

$ git status    *Para obtener información de nuestro repositorio

git add .       *Para empezar el seguimiento de un nuevo archivo...cargar todos los archivos

git commit -m "first commit"   *Commit directo

git push origin master    *Push del branch master

$ git pull origin master    *Actualizar el repositorio

$ git merge arregla-rama         *Fusionar ramas (desde la rama master)

=========en lugar de add y commit -m: git commit -am "Tercer commit"

git branch    *Para ver las rama (s)
git branch rama-nueva   *Crear una rama
git checkout rama-nueva   *Cambiar a la rama

$ git checkout -b arregla-rama   *Cambiar a rama y usarla



========================================================
git log --graph --decorate
git log --graph --decorate --oneline --all





git log    *Para ver el historial de ttodo lo que ha apsado

